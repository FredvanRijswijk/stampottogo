import React from 'react';
import BigHeader from '.';
import mock from './mock.json';

export default {
  title: 'BigHeader',
  component: BigHeader,
};

export const __DefaultSlice = () => < BigHeader slice={mock} />;
