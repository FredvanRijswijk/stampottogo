import React from 'react';
import FlatContent from '.';
import mock from './mock.json';

export default {
  title: 'FlatContent',
  component: FlatContent,
};

export const __DefaultSlice = () => < FlatContent slice={mock} />;
